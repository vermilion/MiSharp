namespace FileStorage.Enums.Features
{
    public enum FileStorageFeatureEnum
    {
        StoreMetaData = 1,
        // note; the next feature should have the int value 4 (1,2,4,8, etc, since they must form a bitwise pattern)
    }
}