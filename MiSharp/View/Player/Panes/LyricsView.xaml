<controls:MetroContentControl x:Class="MiSharp.LyricsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                            xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                                            mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        
        <ScrollViewer Margin="10,10,0,0">
            <TextBlock TextWrapping="Wrap" Text="{Binding LyricsText}"/>
        </ScrollViewer>
        <Button Content="Get Lyrics" Command="{Binding GetLyricsCommand}" HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top" Width="75"/>
    </Grid>
</controls:MetroContentControl>
