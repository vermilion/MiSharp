<controls:MetroContentControl x:Class="MiSharp.PlayerView"
                              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                              xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                              xmlns:cal="http://www.caliburnproject.org" xmlns:miSharp="clr-namespace:MiSharp"
                              xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                              mc:Ignorable="d" d:DesignHeight="60" d:DesignWidth="620">
    <Grid Margin="0,5,0,0">
        <StackPanel Orientation="Horizontal" Margin="0" VerticalAlignment="Top">
            <Button Margin="5,0,5,0" Height="50" FontSize="14" FontWeight="Bold" Content="4" FontFamily="Webdings"
                    ToolTip="Play" HorizontalAlignment="Left" Width="50">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StartClick" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Button Margin="5,0,5,0" Height="50" FontSize="14" FontWeight="Bold" Content=";"
                    FontFamily="Webdings" ToolTip="Pause" HorizontalAlignment="Left" Width="50">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="PauseClick" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Button Margin="5,0,5,0" Height="50" FontSize="14" FontWeight="Bold" Content="&lt;" FontFamily="Webdings"
                    ToolTip="Stop" HorizontalAlignment="Left" Width="50">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StopClick" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <!-- Seek Control -->
            <TextBlock Margin="5,0,5,0" Foreground="Gray" VerticalAlignment="Center" FontSize="10">
                <Run Text="Seek To" />
            </TextBlock>
            <miSharp:SliderWithDraggingEvents x:Name="PositionSlider" Value="{Binding PositionValue}"
                                              TickFrequency="{Binding TickFrequency}"
                                              Maximum="{Binding Maximum}"
                                              VerticalAlignment="Center" Width="223"
                                              Height="Auto" Margin="5,0,5,0">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="ThumbDragCompleted">
                        <cal:ActionMessage MethodName="ChangePosition">
                            <cal:Parameter Value="{Binding Value, ElementName=PositionSlider}" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </miSharp:SliderWithDraggingEvents>
            <TextBlock Foreground="Gray" Margin="5,10.35,5,9.65" VerticalAlignment="Center" FontSize="10">
                <TextBlock.Text>
                    <MultiBinding StringFormat=" {0}/{1}">
                        <Binding Path="CurrentTime" />
                        <Binding Path="TotalTime" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <!-- Volume Control -->
            <TextBlock Foreground="Gray" Margin="5,0,5,0" VerticalAlignment="Center" FontSize="10">
                <Run Text="Volume" />
            </TextBlock>
            <miSharp:SliderWithDraggingEvents x:Name="VolumeSlider" Margin="5,0,5,0" VerticalAlignment="Center"
                                              Width="80" Height="Auto"
                                              Minimum="0" Maximum="1" Value="0.5">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="ThumbDragCompleted">
                        <cal:ActionMessage MethodName="VolumeValueChanged">
                            <cal:Parameter Value="{Binding Value, ElementName=VolumeSlider}" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </miSharp:SliderWithDraggingEvents>
        </StackPanel>
    </Grid>
</controls:MetroContentControl>