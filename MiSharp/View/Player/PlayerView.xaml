<UserControl x:Class="MiSharp.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:cal="http://www.caliburnproject.org" xmlns:miSharp="clr-namespace:MiSharp"
             mc:Ignorable="d" Width="580" Height="83">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/Templates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Border Grid.Column="0" CornerRadius="5,5,5,5" Background="WhiteSmoke" BorderBrush="Gray"
                BorderThickness="4,4,4,4" Margin="13,27,10,0" Height="46" VerticalAlignment="Top">
            <StackPanel Background="Transparent" Width="Auto" Margin="0,0,0,0" Orientation="Horizontal" Height="Auto">
                <Button Width="50" Height="50" Margin="-15,-50,0,0" Template="{StaticResource controlButtonTemplate}"
                        FontSize="14" FontWeight="Bold" Foreground="LightGreen" Content="4" FontFamily="Webdings"
                        ToolTip="Play">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <cal:ActionMessage MethodName="StartClick" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                <Button Width="40" Height="40" Margin="-15,-50,0,0" Template="{StaticResource controlButtonTemplate}"
                        FontSize="14" FontWeight="Bold" Foreground="Orange" Content=";" FontFamily="Webdings"
                        ToolTip="Pause">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <cal:ActionMessage MethodName="PauseClick" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                <Button Width="30" Height="30" Margin="-10,-45,0,0" Template="{StaticResource controlButtonTemplate}"
                        FontSize="14" FontWeight="Bold" Foreground="Yellow" Content="&lt;" FontFamily="Webdings"
                        ToolTip="Stop">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <cal:ActionMessage MethodName="StopClick" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                <!-- Seek Control -->
                <TextBlock Foreground="Gray" Margin="-10,5,5,5" VerticalAlignment="Center" FontSize="10">
                    <Run Text="Seek To" />
                </TextBlock>
                <miSharp:SliderWithDraggingEvents x:Name="PositionSlider" Value="{Binding PositionValue}"
                                                  TickFrequency="{Binding TickFrequency}"
                                                  Maximum="{Binding Maximum}" Margin="2,8,0,8"
                                                  VerticalAlignment="Center" Width="223"
                                                  Height="Auto">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="ThumbDragCompleted">
                            <cal:ActionMessage MethodName="ChangePosition">
                                <cal:Parameter Value="{Binding ElementName=PositionSlider, Path=Value}" />
                            </cal:ActionMessage>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </miSharp:SliderWithDraggingEvents>
                <TextBlock Foreground="Gray" Margin="5,10.35,5,9.65" VerticalAlignment="Center" FontSize="10">
                    <TextBlock.Text>
                        <MultiBinding StringFormat=" {0}/{1}">
                            <Binding Path="CurrentTime" />
                            <Binding Path="TotalTime" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <!-- Volume Control -->
                <TextBlock Foreground="Gray" Margin="5,5,5,5" VerticalAlignment="Center" FontSize="10">
                    <Run Text="Volume" />
                </TextBlock>
                <miSharp:SliderWithDraggingEvents x:Name="VolumeSlider" Margin="2,0,0,0" VerticalAlignment="Center"
                                                  Width="80" Height="Auto"
                                                  Minimum="0" Maximum="1" Value="0.5">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="ThumbDragCompleted">
                            <cal:ActionMessage MethodName="VolumeValueChanged">
                                <cal:Parameter Value="{Binding ElementName=VolumeSlider, Path=Value}" />
                            </cal:ActionMessage>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </miSharp:SliderWithDraggingEvents>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>