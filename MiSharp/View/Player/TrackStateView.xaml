<UserControl x:Class="MiSharp.TrackStateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" MinHeight="40"
             d:DesignHeight="40" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" HorizontalAlignment="Center" TextWrapping="Wrap" Text="{Binding State}" VerticalAlignment="Center" />
        <TextBlock Grid.Column="1" Margin="10,3,10,0" TextWrapping="Wrap" Text="{Binding Track.Title}" VerticalAlignment="Top"/>
        <TextBlock Grid.Column="1" Margin="10,0,10,3" TextWrapping="Wrap" VerticalAlignment="Bottom">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0} | {1} | {2} kbps | {3:hh\:mm\:ss}">
                    <Binding Path="Track.Model.ArtistName" />
                    <Binding Path="Track.Model.AlbumTitle" />
                    <Binding Path="Track.Model.Bitrate" />
                    <Binding Path="Track.Model.Duration" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>   
    </Grid>
</UserControl>
