<controls:MetroContentControl x:Class="MiSharp.TrackView"
                              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                              xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                              xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
                              xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                              mc:Ignorable="d"
                              d:DesignHeight="30" d:DesignWidth="300">
    <Grid>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Add to Now Playing" Command="{Binding AddSongToPlaylistCommand}" />
                <MenuItem Header="Edit Song Information" Command="{Binding EditorEditSongsCommand}" />
            </ContextMenu>
        </Grid.ContextMenu>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" Text="{Binding Model.TrackNumber}" Foreground="#999999"
                   FontFamily="/MiSharp;component/Resources/#Open Sans" />
        <TextBlock Grid.Column="1" Margin="10,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center"
                   Text="{Binding Model.TrackTitle}" FontFamily="/MiSharp;component/Resources/#Open Sans" />
    </Grid>
</controls:MetroContentControl>