<controls:MetroContentControl x:Class="MiSharp.AlbumView"
                              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                              xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
                              xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                              xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                              xmlns:miSharp="clr-namespace:MiSharp"
                              mc:Ignorable="d" MinHeight="160"
                              d:DesignHeight="150" d:DesignWidth="400">
    <Grid x:Name="LblName" Tag="{Binding Title}">
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Add to Now Playing" Command="{Binding AddAlbumToPlaylistCommand}" />
                <MenuItem Header="Edit Album Information" Command="{Binding EditorEditAlbumsCommand}" />
            </ContextMenu>
        </Grid.ContextMenu>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Height="140" Width="140" Source="{Binding Cover}" />

        <StackPanel HorizontalAlignment="Left" Grid.Column="1" VerticalAlignment="Center">
            <TextBlock HorizontalAlignment="Left" Text="{Binding Model.Title}" FontSize="20"
                       FontFamily="/MiSharp;component/Resources/#Open Sans" />
            <TextBlock HorizontalAlignment="Left" Text="{Binding Model.Artist.Name}" FontSize="16"
                       FontFamily="/MiSharp;component/Resources/#Open Sans" />
            <TextBlock HorizontalAlignment="Left" Text="{Binding Model.Year}" FontSize="16"
                       FontFamily="/MiSharp;component/Resources/#Open Sans" />
        </StackPanel>
    </Grid>
</controls:MetroContentControl>